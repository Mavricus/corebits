{
  "name": "@flex-platform/middleware",
  "version": "1.1.0",
  "description": "Flex Platform Middleware package",
  "author": "Mavricus",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "pnpm": ">=9.0.0",
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mavricus/flex-platform/packages/middleware"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "lint:fix": "eslint \"{src,test}/**/*.ts\" --fix",
    "lint:staged": "lint-staged",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@flex-platform/eslint-config": "workspace:*",
    "@flex-platform/typescript-config": "workspace:*"
  }
}
