{
  "name": "@flex-platform/kvs-in-memory",
  "version": "1.1.0",
  "description": "Flex Platform Key-Value Storage Core package",
  "author": "Mavricus",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "pnpm": ">=9.0.0",
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mavricus/flex-platform",
    "directory": "packages/kvs-in-memory"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "lint:fix": "eslint \"{src,test}/**/*.ts\" --fix",
    "lint:staged": "lint-staged",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@flex-platform/base-error": "workspace:^",
    "@flex-platform/kvs-core": "workspace:*"
  },
  "devDependencies": {
    "@flex-platform/eslint-config": "workspace:*",
    "@flex-platform/typescript-config": "workspace:*"
  }
}
