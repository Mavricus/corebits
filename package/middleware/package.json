{
  "name": "@flex/middleware",
  "version": "0.0.1",
  "description": "Flex Platform Middleware package",
  "author": "Mavricus",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "pnpm": ">=9.0.0",
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mavricus/flex-platform"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "precommit": "lint-staged",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@flex/eslint-config": "workspace:*",
    "@flex/typescript-config": "workspace:*"
  }
}
