{
  "name": "@flex/kvs-in-memory",
  "version": "1.0.0",
  "description": "Flex Platform Key-Value Storage Core package",
  "author": "Mavricus",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "pnpm": ">=9.0.0",
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Mavricus/flex-platform"
  },
  "scripts": {
    "build": "rimraf dist && tsc",
    "precommit": "lint-staged",
    "lint": "eslint \"{src,test}/**/*.ts\"",
    "lint:fix": "eslint \"{src,test}/**/*.ts\" --fix",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@flex/base-error": "workspace:^",
    "@flex/kvs-core": "workspace:*"
  },
  "devDependencies": {
    "@flex/eslint-config": "workspace:*",
    "@flex/typescript-config": "workspace:*"
  }
}
